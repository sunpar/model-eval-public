services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: model_eval
      POSTGRES_USER: model_eval
      POSTGRES_PASSWORD: model_eval
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  postgres_data:
